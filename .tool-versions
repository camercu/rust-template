# Pinned tool versions — single source of truth for shell.nix and CI.
# Run `just check-tool-versions` to verify the active environment matches.
rust {{rust_toolchain_version}}
just 1.49.0
cargo-deny 0.19.1
cargo-nextest 0.9.133
typos-cli 1.45.0
taplo-cli 0.10.0
cargo-llvm-cov 0.8.7
nodejs 22.22.2

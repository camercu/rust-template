{
  "name": "{{project-name}}-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Node-based developer tooling for the {{project-name}} Rust project (commitlint).",
  "scripts": {
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1"
  }
}
